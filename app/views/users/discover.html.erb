<h1><%= @user.name %>'s' discover movies page</h1>
<br>
<strong><%= button_to "Discover Top Rated Movies", users_movies_path, method: :get %></strong>
<br>
<strong><%= form_with url: users_movies_path, method: :get, local: true do |f| %>
          <%= f.label :search %>
          <%= f.text_field :search %>
          <%= f.submit 'Search by movie title' %>
          <% end %></strong>